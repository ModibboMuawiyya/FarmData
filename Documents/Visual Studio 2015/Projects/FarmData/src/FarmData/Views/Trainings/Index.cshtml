@model IEnumerable<FarmData.Models.Training>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TDuration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TName)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TDuration)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TName)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.TrainId">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.TrainId">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.TrainId">Delete</a>
        </td>
    </tr>
}
</table>

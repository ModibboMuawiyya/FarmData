@model IEnumerable<FarmData.Models.Cooperatives>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClusterNumber)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClusterNumber)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.CooperativeId">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.CooperativeId">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.CooperativeId">Delete</a>
        </td>
    </tr>
}
</table>
